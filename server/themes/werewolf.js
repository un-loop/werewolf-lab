import { createMuiTheme } from "@material-ui/core";

const werewolf = {
    spacing: 8,
    typography: {
        fontSize: 12,
        body1: {
            fontSize: '1.2rem',
        },
        body2: {
            fontSize: '1rem',
        }
    },
};

export default createMuiTheme(werewolf);
