import React from 'react';

//need to save
//
// at least: handler to send messages to client
// do I need to store the client?
const WebSocketContext = React.createContext(null);

export default WebSocketContext;
